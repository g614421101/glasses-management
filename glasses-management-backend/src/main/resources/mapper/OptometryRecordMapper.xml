<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glasses.mapper.OptometryRecordMapper">

    <select id="selectAnyById" resultType="com.glasses.entity.OptometryRecord">
        SELECT * FROM optometry_record WHERE id = #{id}
    </select>

    <select id="selectDeletedList" resultType="com.glasses.entity.OptometryRecord">
        SELECT * FROM optometry_record WHERE deleted = true ORDER BY deleted_time DESC, id DESC
    </select>

    <select id="selectDeletedByCustomerId" resultType="com.glasses.entity.OptometryRecord">
        SELECT * FROM optometry_record WHERE customer_id = #{customerId} AND deleted = true
    </select>

    <update id="softDeleteById">
        UPDATE optometry_record SET deleted = true, deleted_time = #{deletedTime}, deleted_by = #{deletedBy} WHERE id = #{id} AND deleted = false
    </update>

    <update id="softDeleteByCustomerId">
        UPDATE optometry_record SET deleted = true, deleted_time = #{deletedTime}, deleted_by = #{deletedBy} WHERE customer_id = #{customerId} AND deleted = false
    </update>

    <update id="restoreByIdIgnoringLogic">
        UPDATE optometry_record SET deleted = false, deleted_time = NULL, deleted_by = NULL WHERE id = #{id}
    </update>

    <update id="restoreByCustomerIdIgnoringLogic">
        UPDATE optometry_record SET deleted = false, deleted_time = NULL, deleted_by = NULL WHERE customer_id = #{customerId} AND deleted = true
    </update>

    <delete id="physicalDeleteById">
        DELETE FROM optometry_record WHERE id = #{id} AND deleted = true
    </delete>

    <delete id="physicalDeleteByCustomerId">
        DELETE FROM optometry_record WHERE customer_id = #{customerId} AND deleted = true
    </delete>

    <delete id="physicalDeleteExpired">
        DELETE FROM optometry_record WHERE deleted = true AND deleted_time &lt; #{expireBefore}
    </delete>

    <select id="selectAllIncludingDeleted" resultType="com.glasses.entity.OptometryRecord">
        SELECT * FROM optometry_record
    </select>

    <select id="findByCustomerAndExamDate" resultType="com.glasses.entity.OptometryRecord">
        SELECT * FROM optometry_record WHERE customer_id = #{customerId} AND exam_date &gt;= #{examDateStart} AND exam_date &lt; #{examDateEnd}
    </select>

    <delete id="deleteAll">
        DELETE FROM optometry_record
    </delete>

</mapper>
